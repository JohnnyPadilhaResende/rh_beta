<%= form_with(model: core_instruco, local: true) do |form| %>
  <% if core_instruco.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(core_instruco.errors.count, "error") %> prohibited this core_instruco from being saved:</h2>

      <ul>
      <% core_instruco.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :cdg_grau_instrucao %>
    <%= form.number_field :cdg_grau_instrucao, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :nme_curso %>
    <%= form.text_field :nme_curso, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :nme_instituicao_ensino %>
    <%= form.text_field :nme_instituicao_ensino, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if core_instruco.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', core_instruco, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if core_instruco.persisted? %>
      <%= link_to "Cancel", core_instruco, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", core_instrucoes_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
