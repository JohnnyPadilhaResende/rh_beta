<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <thead>
      <tr>
        <th>Servidor</th>
        <th>Telefone</th>
        <th>Email</th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @contatos.each do |contato| %>
        <%= content_tag :tr, id: dom_id(contato), class: dom_class(contato) do %>
                      <td><%= contato.core_pessoal.nme_pessoa %></td>
                      <td><%= contato.end_telefone %></td>
                      <td><%= contato.end_email %></td> 
                      <td><%= link_to 'Destroy', contato, method: :delete, class: "text-danger", data: { confirm: 'Você tem certeza?' } %></td>              
        <% end %>
      <% end %>
    </tbody>
  </table>
</div>