<%= form_with(model: core_endereco, local: true) do |form| %>
  <% if core_endereco.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(core_endereco.errors.count, "error") %> prohibited this core_endereco from being saved:</h2>

      <ul>
      <% core_endereco.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :cdg_ordem %>
    <%= form.number_field :cdg_ordem, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :nme_logradouro %>
    <%= form.text_field :nme_logradouro, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :cdg_logradouro %>
    <%= form.number_field :cdg_logradouro, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :nmr_lote %>
    <%= form.number_field :nmr_lote, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :nmr_quadra %>
    <%= form.number_field :nmr_quadra, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :desc_complemento %>
    <%= form.text_field :desc_complemento, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :nmr_cep %>
    <%= form.number_field :nmr_cep, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if core_endereco.persisted? %>
      <div class="float-right">
        <%= link_to 'Destroy', core_endereco, method: :delete, class: "text-danger", data: { confirm: 'Are you sure?' } %>
      </div>
    <% end %>

    <%= form.submit class: 'btn btn-primary' %>

    <% if core_endereco.persisted? %>
      <%= link_to "Cancel", core_endereco, class: "btn btn-link" %>
    <% else %>
      <%= link_to "Cancel", core_enderecos_path, class: "btn btn-link" %>
    <% end %>
  </div>
<% end %>
